services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      # postgres 18's official image stores data under a major-version
      # subdirectory of /var/lib/postgresql (not .../data as before) so it
      # can support pg_upgrade --link across major versions.
      - pgdata:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  bot:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      TILE_CACHE_DIR: /app/.tilecache
    volumes:
      - tilecache:/app/.tilecache
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
  tilecache:
